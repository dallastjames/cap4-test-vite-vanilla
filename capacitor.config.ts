import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dallastjames.dev.cap4nonionic',
  appName: 'cap4-non-ionic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
